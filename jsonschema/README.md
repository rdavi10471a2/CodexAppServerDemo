# Codex app-server JSON schemas

This folder is reserved for generated `codex app-server` JSON schema reference files.

Generate or refresh the schemas with:

```powershell
codex app-server generate-json-schema --out .\jsonschema
```

The schemas are used as protocol reference material for the Blazor harness and tests. They are not runtime inputs for the app.
